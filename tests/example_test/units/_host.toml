# This will be the events checked for the host 
["_host"]
init = 0
use_setup = false
in-except = "Resume" # Resume or Reload or Resume-ALL
unit_dependencies = []
events = ["event1", "event2", "event3"]